import { ButtonStyleType } from './types';

export const variantClasses: Record<ButtonStyleType, string> = {
    primary: 'btn btn-primary btn-outline text-primary-content',

    link: 'btn btn-link text-base-content',

    secondary: 'btn  btn-ghost text-base-content',
};
